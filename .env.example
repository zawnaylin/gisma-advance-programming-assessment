# Copy to .env and adjust before running `docker compose up -d --build`.
POSTGRES_DB=qr_restaurant
POSTGRES_USER=qr_restaurant
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432
APP_PORT=8080
# Public URL of the app; encoded into table QR codes, so use the host's LAN IP or domain.
APP_BASE_URL=http://192.168.20.43:8080
